<!DOCTYPE html>
<html lang="en">
	<head>
	    <!-- Required meta tags -->
	    <meta charset="utf-8">
	    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<link rel="icon" type="image/png" href="{{ base_url() }}/img/favicon.ico">

	   <!-- Bootstrap CSS -->
	   <link rel="stylesheet" type="text/css" href="{{ base_url() }}/css/bootstrap.min.css">

		<!-- Mis estilos css -->
		<link rel="stylesheet" type="text/css" href="{{ base_url() }}/css/estilosPrint.css">
		<title>{{ titulo }}</title>
		<style type="text/css">
			.cellRight {
			  text-align: right;
			}
		</style>
	</head>
	<body>
		<header>
			<div class="container-fluid">
				<div class="row justify-content-end">
					<div class="col-xs-3">
						<button id="btnImprimir" type="button" class="btn btn-primary mr-1">Imprimir</button>
						<button id="btnVolver" type="button" class="btn btn-light mr-1">Volver</button>
					</div>
				</div>
				<div class="row">
					<div class="col">
						<hr>
					</div>
				</div>
			</div>
		</header>

		<div class="container-fluid">
			<div class="row">
				<div class="col pr-5 mb-0 mr-5 mt-3">
					<p class="text-right mb-0">Fecha emisión: {{ fecha }}</p>
				</div>
			</div>

			<div class="row">
				<div class="col mb-3">
					<h3 class="text-center" id="titImpGuia"><strong>Listado visitas a cliente, suma por producto</strong></h3>
					<p class="text-center">{{ entrefechas }}</p>
				</div>
			</div> {# end row #}

			{% set idx = 0 %}
			{% for data in dataCli %}

				<div class="row">
					<div class="col-3"></div>
					<div class="col mb-4">

					<h6><strong>{{ client[idx].IdCliente }} - {{ client[idx].ApellidoNombre }}</strong> <small>({{ client[idx].Direccion }})</small></h6>

						<table class="table table-striped table-bordered table-sm">
							<thead class="thead-dark">
								<tr class="small">
									<th scope="col" class="cellRight">Id</th>
							        <th scope="col" class="text-center">Producto</th>
							        <th scope="col" class="cellRight" style="width: 75px;">Dejado</th>
							        <th scope="col" class="cellRight" style="width: 75px;">Retirado</th>
						    	</tr>
							</thead>
							<tbody>
								{% for datcli in data %}
									<tr class="small">
										<td class="cellRight">{{ datcli.IdProducto }}</td>
										<td>{{ datcli.Descripcion }}</td>
										<td class="cellRight">{{ datcli.Dejado }}</td>
										<td class="cellRight">{{ datcli.Retirado }}</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
					<div class="col-3"></div>
				</div>

			{% set idx = idx + 1 %}
            {% endfor %}


		</div>  {# end container #}

		<script src="{{ base_url() }}/js/jquery-3.3.1.min.js"></script>
		<script type="text/javascript">
			// Codigo jQuery
			$(document).ready(function(){

				// Para boton volver
				$('#btnVolver').click( function() {
			    	window.close();
			    });

				// Boton Imprimir
				$('#btnImprimir').click( function() {
                    window.print();
				});
			});
		</script>
	</body>
</html>
